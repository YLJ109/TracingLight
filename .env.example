# 溯光 TracingLight 环境变量配置

# ========== 智谱 AI ==========
# 在 https://open.bigmodel.cn 注册获取 API Key
ZHIPU_API_KEY=your_zhipu_api_key_here
# 模型选择: glm-4-flash (免费/推荐) | glm-4-plus (付费/更强)
ZHIPU_MODEL=glm-4-flash

# ========== JWT 认证 ==========
# 随机字符串，用于签发和验证 JWT Token
JWT_SECRET=change_this_to_a_random_secret_string

# ========== 数据库 ==========
# 驱动固定为 PostgreSQL
DATABASE_DRIVER=postgres
# 默认连接本机/本地 PostgreSQL（端口 5433）。两种启动方式：
#   1) 直接启动: 你有自己的本机 PostgreSQL，监听 localhost:5433
#      （执行 setup.bat / start.bat）
#   2) Docker 管理: 用 docker compose 起一个 pg 容器，映射到宿主机 5433
#      （执行 docker/setup-docker.bat / docker/start-docker.bat，均在 docker/ 目录）
# 若你的 PostgreSQL 在 5432，把链接的 5433 改成 5432 即可。
DATABASE_URL=postgres://tracinglight:tracinglight_pw@localhost:5433/tracinglight

# ========== 服务 ==========
NODE_ENV=production
PORT=5000

# ========== 静态/上传存储（默认本地磁盘，零配置） ==========
# 切到 S3 兼容对象存储时：
#   STORAGE_DRIVER=s3
#   S3_ENDPOINT=https://minio.local:9000         # 或 OSS/COS/R2 等 S3 兼容端点
#   S3_REGION=us-east-1
#   S3_ACCESS_KEY=xxx
#   S3_SECRET_KEY=xxx
#   S3_BUCKET=tracinglight
#   S3_PUBLIC_URL=https://cdn.example.com        # 可选，生成对外 URL 的基址（默认用 endpoint）
# STORAGE_DRIVER=local

# ========== 短缓存 ==========
# 学情聚合接口的缓存 TTL（毫秒），0=关闭缓存
# CACHE_TTL_MS=8000

# Cookie 的 Secure 开关（HTTP 部署必须设 false，否则浏览器拒存登录 cookie → 401）
#   跳 HTTPS 对外: COOKIE_SECURE=true
#   内部服务器 / Coze / 裸 HTTP: COOKIE_SECURE=false
#   不设置时: 生产=开启 secure，开发=关闭
# COOKIE_SECURE=false
